{
  vim.languages = {
    bash = {
      enable = true;
      format.enable = true;
      lsp.enable = true;
    };

    clang = {
      enable = true;
      lsp.enable = true;
    };

    css = {
      enable = true;
      format.enable = true;
      lsp.enable = true;
    };

    go = {
      enable = true;
      format.enable = true;
      lsp.enable = true;
    };

    lua = {
      enable = true;
      format.enable = true;
      lsp.enable = true;
    };

    markdown = {
      enable = true;
      format.enable = true;
      lsp.enable = true;
    };

    python = {
      enable = true;
      format.enable = true;
      lsp.enable = true;
    };

    rust = {
      enable = true;
      format.enable = true;
      lsp.enable = true;
    };
  };
}