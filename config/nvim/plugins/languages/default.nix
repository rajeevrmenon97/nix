{
  vim.languages = {
    bash = {
      enable = true;
      lsp.enable = true;
    };

    clang = {
      enable = true;
      lsp.enable = true;
    };

    css = {
      enable = true;
      lsp.enable = true;
    };

    go = {
      enable = true;
      lsp.enable = true;
    };

    lua = {
      enable = true;
      lsp.enable = true;
    };

    markdown = {
      enable = true;
      lsp.enable = true;
    };

    python = {
      enable = true;
      lsp.enable = true;
    };

    rust = {
      enable = true;
      lsp.enable = true;
    };
  };
}